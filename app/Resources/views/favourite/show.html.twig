{% extends 'base.html.twig' %}

{% block body %}
    <h1>Favourite</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ favourite.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('favourite_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('favourite_edit', { 'id': favourite.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
